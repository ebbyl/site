---
title: HTTPS vs HTTP
description: HTTP Overview
---

import { Aside, Card } from "@astrojs/starlight/components";

<Card title="HTTPS" icon="star">
  A security feature used to send encrypted messages.
</Card>

---

The API uses HTTPS instead HTTP. HTTP messages are sent over the wire  
as plaintext which means they can be easily intercepted and read. HTTPS
provides safer messaging by encrypting the data in-transit.

---
